<header>
	<img height="36" src="favicon.ico" style="margin-right: 24px" />
	<h2>TEA</h2>
	<div style="flex: 1"></div>
	<%if (isLogin) { %>
		<a href="/logout" class="logout">Logout</a>
	<% } %>
</header>
<style>
	header {
		display: flex;
		height: 64px;
		width: 100%;
		padding: 0 24px;
		box-sizing: border-box;
		align-items: center;
		color: #fff;
		background-color: var(--primary-color-500);
	}
	a.logout {
		height: 32px;
		line-height: 30px;
		padding: 0 12px;
		border: 1px solid currentColor;
		color: inherit;
		text-decoration: none;
		border-radius: 4px;
	}
</style>
